/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TransormFieldToNamePipe } from './transorm-field-to-name.pipe';

describe('Pipe: TransormFieldToName', () => {
  it('create an instance', () => {
    let pipe = new TransormFieldToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
