/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Indicator} from './indicator';

describe('Indicator', () => {
  it('should create an instance', () => {
    expect(new Indicator()).toBeTruthy();
  });
});
